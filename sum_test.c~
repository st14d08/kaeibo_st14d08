#include <stdio.h>
#include"sum.h"

 int main(void)
{
   int payments[] = { 10, 90, 50, 30, 70, 50};

  int n = sizeof(payments) / sizeof(payments[0]);

 printf("合計は%d\n", sum(payments,n));


 return 0;

}
